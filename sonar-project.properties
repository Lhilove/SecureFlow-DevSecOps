sonar.projectKey=5792ef83fa4e0a2ae0a1c1bec720a080eaa16c9b
sonar.organization=lhilove

sonar.sources=services
sonar.exclusions=**/tests/**,**/*.md,**/migrations/**

sonar.python.version=3.10

# These are intentionally-vulnerable services (see VULNERABILITIES.md).
# The security gate step in the pipeline (issue #10) is what should block
# merges on findings; SonarCloud's own quality gate can stay informational
# for now since a from-scratch SAST baseline on a deliberately vulnerable
# app will start red by design.